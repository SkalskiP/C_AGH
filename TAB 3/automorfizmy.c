#include <stdio.h>

int weryfikacja(int liczba, int sqr) {
	if 
}

int main (void) {
	
	int index, lim;
	
	printf("Podaj granice analizowanego przedzialu: ");
	scanf("%d", &lim);
	
	for (index = 2; index <= lim; index++) {
		int sqr = index*index;
		
	
	}
	
	return 0;
}
